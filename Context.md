# Project Context — Local Persian Document Translator

## هدف پروژه
ساخت یک اپلیکیشن محلی برای ترجمهٔ اسناد و متن بین فارسی، انگلیسی و عربی، با تمرکز اصلی بر ترجمهٔ باکیفیت انگلیسی به فارسی. برنامه باید فایل‌های TXT، DOCX و PDF متنی را بگیرد، ساختار سند را تا حد ممکن حفظ کند و خروجی قابل دانلود بدهد.

## وضعیت نسخهٔ قبلی
نسخهٔ فعلی با Flask و `llama-cpp-python` ساخته شده است و از مدل‌های GGUF محلی استفاده می‌کند. قابلیت‌های Job، صف، توقف امن، Resume از checkpoint، تاریخچهٔ مراحل، وضعیت مدل، خروجی DOCX/PDF و فونت Vazirmatn در آن پیاده‌سازی شده‌اند.

اما نتیجهٔ ترجمه رضایت‌بخش نبوده است:

- Gemma 4B در ترجمهٔ فارسی کیفیت و ثبات کافی ندارد.
- اصطلاحات تکراری ناپایدار ترجمه می‌شوند؛ مثال واقعی:
  - `black swan` یک‌بار «اردک سیاه» و یک‌بار «خرس سیاه» شده است.
  - ترجمهٔ درست در بیشتر کاربردهای عمومی و نظری: «قوی سیاه».
- فرایند سه‌مرحله‌ای ترجمه، ویرایش و کنترل کیفیت بسیار کند بود.
- کنترل کیفیت سخت‌گیرانه گاهی در حدود ۹۲٪ کل Job را متوقف می‌کرد.
- خطاهایی مانند تفاوت ارقام فارسی و لاتین یا واژه‌نامهٔ تولیدشده توسط خود مدل، مانع تولید فایل خروجی می‌شدند.
- اصلاح اخیر این مشکل را تا حدی کاهش داد: اعداد فارسی/لاتین نرمال‌سازی شدند، واژه‌نامهٔ خودکار دیگر اجباری نیست، حالت پیش‌فرض `balanced` تعداد فراخوانی‌های مدل را کم می‌کند و خروجی با هشدار قابل دانلود می‌شود. با این حال کیفیت بنیادی ترجمه هنوز مطلوب نیست.

## نتیجهٔ مهم
راه‌حل صرفاً بزرگ‌تر کردن مدل فعلی نیست. معماری باید از «یک LLM کوچک که همهٔ کارها را انجام می‌دهد» به یک خط لولهٔ تخصصی تغییر کند:

1. موتور ترجمهٔ ماشینی تخصصی برای تولید پیش‌نویس کامل و یکنواخت.
2. مدل زبانی قوی برای بازبینی معنایی، اصلاح سبک فارسی، کنترل اصطلاحات و تصمیم‌گیری دربارهٔ موارد مشکوک.
3. کنترل‌های قطعی برای اعداد، تاریخ‌ها، URL، ایمیل، نام‌ها، جدول‌ها و ساختار فایل.
4. فقط بخش‌های مشکوک دوباره به مدل بازبین فرستاده شوند؛ کل سند چندبار ترجمه نشود.

## ایده‌های مطرح‌شده برای موتور ترجمهٔ اولیه

### Argos Translate
Repository:
https://github.com/argosopentech/argos-translate

ایده: استفاده از Argos Translate برای ترجمهٔ اولیه و تولید پیش‌نویس کامل. مزیت آن اجرای آفلاین و تخصصی‌بودن در ترجمه است. باید کیفیت واقعی آن برای مسیر English → Persian با نمونه‌های واقعی سنجیده شود؛ نباید صرفاً براساس آفلاین‌بودن انتخاب شود.

### MADLAD-400
Model:
https://huggingface.co/google/madlad400-10b-mt

این مدل یک مدل تخصصی ترجمهٔ چندزبانه است و گزینهٔ جدی‌تری برای ترجمهٔ اولیه نسبت به یک LLM عمومی محسوب می‌شود. برای اجرا به نسخهٔ سازگار با GPU، حافظه و backend مناسب نیاز دارد. باید سرعت و کیفیت آن روی متن‌های فارسی واقعی آزموده شود.

## گزینه‌های مطرح‌شده برای بازبین فارسی

- Dorna Llama 3 8B:
  https://huggingface.co/PartAI/Dorna-Llama3-8B-Instruct-GGUF
  https://ollama.com/partai/dorna-llama3

- Gemma 3 Persian:
  https://ollama.com/mshojaei77/gemma3persian

- Maral:
  https://huggingface.co/MaralGPT/Maral-7B-alpha-1

- مدل‌های Mistral یا مدل‌های محلی Ollama نیز باید به‌عنوان بازبین بررسی شوند.

## رویکرد پیشنهادی برای نسخهٔ بعد

### مرحله ۱: استخراج ساختار
- ورودی: TXT، DOCX یا PDF متنی.
- استخراج پاراگراف‌ها، Headingها، جدول‌ها، Runهای دارای bold/italic/hyperlink و metadata.
- هر بخش یک شناسهٔ پایدار داشته باشد.
- ساختار و متن اصلی هرگز با مدل جایگزین نشود.

### مرحله ۲: تحلیل سند و واژه‌نامهٔ اولیه
- یک مدل بازبین فارسی یا مدل ترجمهٔ منتخب، فقط ابتدای سند و نمونه‌هایی از سراسر آن را بررسی کند.
- خروجی:
  - حوزهٔ متن
  - لحن
  - شخصیت‌ها و نام‌ها
  - اصطلاحات تکراری
  - ترجمهٔ پیشنهادی اصطلاحات
- واژه‌نامهٔ خودکار فقط پیشنهادی باشد.
- واژه‌نامه‌ای که کاربر تأیید یا وارد می‌کند، قفل‌شده و اجباری باشد.

### مرحله ۳: ترجمهٔ اولیه تخصصی
- کل متن به بخش‌های بزرگ و منطقی تقسیم شود.
- موتور ترجمهٔ ماشینی منتخب، پیش‌نویس همهٔ بخش‌ها را تولید کند.
- متن‌های تکراری با Translation Memory یک‌بار ترجمه شوند.
- اصطلاحات قفل‌شده در ورودی مدل اعمال شوند.

### مرحله ۴: تشخیص بخش‌های مشکوک
بدون LLM، موارد زیر بررسی شوند:

- حذف یا تغییر اعداد، تاریخ‌ها، درصدها و واحدها
- URL و ایمیل
- نام‌های خاص
- طول غیرعادی ترجمه
- وجود متن ترجمه‌نشده
- تضاد در ترجمهٔ اصطلاحات تکراری
- شکست ساختار JSON یا تعداد Segmentها

فقط بخش‌هایی که در این مرحله مشکوک هستند، برای بازبینی سنگین ارسال شوند.

### مرحله ۵: بازبینی فارسی با LLM
مدل بازبین نباید سند را از صفر ترجمه کند. باید این ورودی را بگیرد:

- متن اصلی
- ترجمهٔ اولیه
- واژه‌نامهٔ قفل‌شده
- حافظهٔ اصطلاحات قبلی
- دلیل ارجاع، مثل `inconsistent term` یا `number mismatch`

خروجی باید فقط نسخهٔ اصلاح‌شدهٔ همان Segmentها باشد.

وظایف بازبین:

- طبیعی‌کردن فارسی
- حفظ کامل معنا
- یکسان‌سازی اصطلاحات
- حفظ لحن
- جلوگیری از ترجمهٔ کلمه‌به‌کلمه
- جلوگیری از تبدیل مفاهیم به مفاهیم دیگر
- اصلاح خطاهای ترجمهٔ اولیه

### مرحله ۶: کنترل نهایی و خروجی
- کنترل قطعی دوباره اجرا شود.
- اگر هشدار غیرقطعی وجود دارد، فایل تولید شود و هشدار به کاربر نشان داده شود.
- فقط خطاهای ساختاری یا از‌دست‌رفتن دادهٔ قطعی باید خروجی را متوقف کنند.
- DOCX باید Heading، جدول، Run، جهت RTL و فونت Vazirmatn را حفظ کند.
- PDF باید با فونت:
  `./font/Vazirmatn-VariableFont_wght.ttf`
  تولید شود.

## معیار پذیرش نسخهٔ جدید
نسخهٔ جدید فقط وقتی قابل قبول است که روی چند سند واقعی انگلیسی به فارسی این موارد را نشان دهد:

- اصطلاح تکراری در کل سند یک ترجمهٔ ثابت دارد.
- اعداد، تاریخ‌ها، واحدها و نام‌ها حفظ می‌شوند.
- خروجی فارسی طبیعی و قابل انتشار است.
- کاربر در پایان همیشه فایل خروجی می‌گیرد، مگر فایل ورودی یا ساختار خروجی واقعاً خراب باشد.
- Jobهای طولانی مرحله، مدل فعال، زمان آخرین فعالیت و علت توقف را شفاف نشان می‌دهند.
- سرعت نسبت به معماری قبلی بهتر باشد؛ بازبین LLM فقط روی بخش‌های لازم اجرا شود.
- کیفیت هر موتور ترجمه و هر مدل بازبین با مجموعه‌ای از نمونه‌های واقعی سنجیده شود، نه براساس حدس یا شهرت مدل.

## محدودیت‌ها و ترجیحات
- اجرای محلی و آفلاین اولویت دارد.
- وابستگی به API خارجی و CDN نامطمئن مطلوب نیست.
- سخت‌افزار موجود: RTX 4060 Ti با 16GB RAM سیستم، CPU i5-4460 و SSD SATA. بنابراین مدل‌ها باید از نظر VRAM، RAM و سرعت واقعی ارزیابی شوند.
- از AirLLM استفاده نشود؛ اجرای لایه‌به‌لایه از دیسک برای ترجمهٔ طولانی کند است.
- برای مدل‌های جدید، Ollama یا backend مناسب CUDA می‌تواند از `llama.cpp` مناسب‌تر باشد، اما انتخاب نهایی باید پس از benchmark انجام شود.

## اولین کار در گفت‌وگوی جدید
1. منابع رسمی Argos Translate، MADLAD-400، Dorna، Gemma3 Persian و Maral را بررسی کن.
2. با توجه به English → Persian، RTX 4060 Ti و 16GB RAM، یک ماتریس انتخاب شامل کیفیت، سرعت، VRAM، نصب، آفلاین‌بودن و سازگاری فارسی بساز.
3. یک معماری نهایی انتخاب کن.
4. قبل از پیاده‌سازی، یک مجموعهٔ benchmark کوچک و واقعی برای سنجش کیفیت ترجمه طراحی کن.
5. سپس برنامه را از معماری قبلی به خط لولهٔ «ترجمهٔ تخصصی + بازبینی انتخابی LLM» منتقل کن.